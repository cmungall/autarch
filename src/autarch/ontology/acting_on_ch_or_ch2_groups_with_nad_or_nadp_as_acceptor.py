"""acting on ch or ch2 groups with nad(+) or nadp(+) as acceptor."""

from autarch.ontology.ec_prefix_aggregate import ExplicitEcAggregate


class ActingOnCHOrCH2GroupsWithNADOrNADPAsAcceptor(ExplicitEcAggregate):
    """acting on ch or ch2 groups with nad(+) or nadp(+) as acceptor."""

    EC_NUMBER_PREFIX = '1.17.1.-'
    CHILD_CLASSES = ()
