"""acting on iron-sulfur proteins as donors with nad(+) or nadp(+) as acceptor."""

from autarch.ontology.ec_prefix_aggregate import ExplicitEcAggregate


class ActingOnIronSulfurProteinsAsDonorsWithNADOrNADPAsAcceptor(ExplicitEcAggregate):
    """acting on iron-sulfur proteins as donors with nad(+) or nadp(+) as acceptor."""

    EC_NUMBER_PREFIX = '1.18.1.-'
    CHILD_CLASSES = ()
