"""oxidizing metal ions with nad(+) or nadp(+) as acceptor."""

from autarch.ontology.ec_prefix_aggregate import ExplicitEcAggregate


class OxidizingMetalIonsWithNADOrNADPAsAcceptor(ExplicitEcAggregate):
    """oxidizing metal ions with nad(+) or nadp(+) as acceptor."""

    EC_NUMBER_PREFIX = '1.16.1.-'
    CHILD_CLASSES = ()
